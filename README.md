# Финансовый помощник

Веб-приложение для управления личными финансами с возможностью установки как PWA на мобильные устройства.

## Технологии

- **React 18** - библиотека для создания пользовательского интерфейса
- **TypeScript** - типизация кода
- **Rspack** - быстрый сборщик для React приложений
- **CSS Modules** - изолированные стили для компонентов
- **PWA** - возможность установки как веб-приложение

## Функциональность

- 📊 **Дашборд** с отображением баланса, доходов и расходов
- 💰 **Учет операций** с категориями (доходы и расходы)
- 📅 **Планируемые траты** с возможностью выбора категории
- 🏷️ **Категории** для группировки операций
- 🔍 **Фильтрация** операций по категориям
- 💾 **Локальное хранение** данных в браузере

## Установка и запуск

1. Установите зависимости:
```bash
npm install
```

2. Запустите сервер разработки:
```bash
npm run dev
```

3. Откройте браузер по адресу `http://localhost:3000`

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

## Установка как PWA

Приложение поддерживает установку как Progressive Web App:

1. Создайте иконки для PWA (192x192 и 512x512 пикселей) и поместите их в папку `public/` как `icon-192.png` и `icon-512.png`
2. Откройте приложение в браузере
3. В мобильных браузерах появится предложение установить приложение
4. Или используйте меню браузера: "Установить приложение"

## Структура проекта

```
src/
  ├── components/       # React компоненты
  │   ├── Dashboard.tsx
  │   ├── TransactionList.tsx
  │   ├── PlannedExpenses.tsx
  │   ├── TransactionForm.tsx
  │   └── CategoryFilter.tsx
  ├── context/          # React Context для состояния
  │   └── FinanceContext.tsx
  ├── App.tsx           # Главный компонент
  ├── index.tsx         # Точка входа
  └── index.css         # Глобальные стили
```

## Особенности

- Все тексты на русском языке
- Работа только с рублями (₽)
- Адаптивный дизайн для мобильных устройств
- CSS Modules для изоляции стилей
- Нет раздела настроек
